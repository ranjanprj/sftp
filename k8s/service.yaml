apiVersion: v1
kind: Service
metadata:
  name: sftp-service
spec:
  selector:
    app: sftp-server
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 2222
      targetPort: 2222
      name: sftp-main-port

